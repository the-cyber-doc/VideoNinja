version: '3.8'

services:
  app:
    build: .
    container_name: videoninja
    ports:
      - "8000:8000"
    volumes:
      - ./uploads:/app/uploads
      - ./main.py:/app/main.py
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
